#pragma once

class SDL_Texture tex;

class Dialogue {
	//query the texture based on letters in the string

public:
	//initialize tex if not initialized
	SDL_Texture* textBox;
	Dialogue() {		
	}	
	// Parameters:
	// string / char array - text to convert into a texture

};