#pragma once
class TeleportTile
{

	//when touched, an action occurs.
	//we want this action to be generic. so 

public:
	TeleportTile();
	~TeleportTile();
 
	void OnTeleport( );

		
	//initialize the list of teleport tiles when a map loads.
	//different locations depending on the background. they will change based on the resolution



	//so do we want these teleport times to be 

	//loop and check these objects that are loaded in load level and theres a vector on the game
	//
	//

};

